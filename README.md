# Experimental blog project

### Set up:
Create a .env file and fill with custom values: `cp .env.example .env`

### Connection testing:
1. Start the server package from root via `pnpm start:server` command
2. Visit ***http://127.0.0.1:3333/health*** or your custom server address
